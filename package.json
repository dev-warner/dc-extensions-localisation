{
  "name": "dc-extension-localise",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "dc-extensions-sdk": "^1.0.1",
    "netlify-cli": "^2.21.0",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start:all": "npx netlify dev",
    "serve:lambda": "npx netlify-lambda serve src/lambda",
    "build:lambda": "npx netlify-lambda build src/lambda",
    "postinstall": "npx netlify-lambda install src/lambda"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/preset-env": "^7.7.1",
    "http-proxy-middleware": "^0.20.0",
    "netlify-lambda": "^1.6.3"
  }
}
